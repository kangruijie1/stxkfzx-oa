<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="xyz.stxkfzx.manager.user.mapper.UserMapper">

	<insert id="insertUser"  useGeneratedKeys="true" keyProperty="userId" >
		insert into user(departmentId, jobId, username)
		values(#{departmentId}, #{jobId}, #{username})
	</insert>

	<select id="selectUser" resultType="xyz.stxkfzx.manager.user.pojo.TUser">
		select * from user
		<where>
			<if test="departmentId != null and departmentId != ''">
				and departmentId = #{departmentId}
			</if>
			<if test="jobId != null and jobId != ''">
				and jobId = #{jobId}
			</if>
			<if test="username != null and username != ''">
				and username = #{username}
			</if>
		</where>
	</select>

	<select id="selectUserTest" resultType="String">
		select username from test
	</select>
</mapper>